using UnityEngine;
using Telestrations.Server;

namespace Telestrations
{
    public class Initialization : MonoBehaviour
    {
        /*private Server _server;

        private void Awake()
        {
            _server = new Server();

            ServerBroadcaster serverBroadcaster = new ServerBroadcaster();

            ServerFinder serverFinder = new ServerFinder();
        }

        private void OnDestroy()
        {
            _server.Dispose();
        }*/
    }
}
